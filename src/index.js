import React from "react"
import ReactDOM from "react-dom"
import App from "./App"
import "../src/style.css"

ReactDOM.render(<App />, document.getElementById("root"))

// add hover to all links and color change